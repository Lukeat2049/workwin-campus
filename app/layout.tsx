import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-sans",
  display: "swap",
});

export const metadata: Metadata = {
  title: "WorkWin Campus — Turn student experience into career-ready stories",
  description:
    "A career-readiness platform that helps students turn internships, projects, jobs, volunteer work, and leadership into resume bullets, interview stories, LinkedIn language, and elevator pitches.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={inter.variable}>
      <body>{children}</body>
    </html>
  );
}
